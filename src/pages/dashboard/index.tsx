import React from "react";
import Layout from "@/components/layout/layout";
import Chart from "@/components/ui/dashboard/dashboard";

const Dasboard = () => {
  return (
    <Layout>
      <Chart />
    </Layout>
  );
};

export default Dasboard;
