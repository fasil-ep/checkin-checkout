import React from "react";
import Layout from "@/components/layout/layout";
import DataTable from "@/components/ui/detail-table/detail-table";

const index = () => {
  return (
    <Layout>
      <DataTable />
    </Layout>
  );
};

export default index;
