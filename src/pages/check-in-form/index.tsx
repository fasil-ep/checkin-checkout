import React from "react";
import Layout from "@/components/layout/layout";
import CheckInForm from "@/components/ui/check-in-form/check-in-form";

const index = () => {
  return (
    <Layout>
      <CheckInForm />
    </Layout>
  );
};

export default index;
